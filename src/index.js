import React from "react";
import ReactDOM from "react-dom";

const custStyle = { color: "brown", fontSize: "20px" };

ReactDOM.render(
  <h1 style={custStyle}>Hello World!</h1>,
  document.getElementById("root")
);
